---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getEntry, render } from 'astro:content';

const page = await getEntry('pages', 'priser');
const { Content } = await render(page);
---

<BaseLayout title={page.data.title} description={page.data.description}>
  <section class="max-w-4xl mx-auto px-4 py-16">
    <h1 class="text-4xl font-bold text-brand-700 mb-8" style="font-family: 'Josefin Sans', sans-serif;">{page.data.title}</h1>
    <div class="bg-white/70 backdrop-blur-sm rounded-2xl p-8 border border-brand-100">
      <div class="prose prose-lg max-w-none prose-headings:text-brand-700 prose-a:text-brand-500 prose-th:bg-brand-50 prose-th:text-brand-700 prose-th:p-3 prose-th:text-left prose-td:p-3 prose-td:border-t prose-td:border-brand-100">
        <Content />
      </div>
    </div>
  </section>
</BaseLayout>
